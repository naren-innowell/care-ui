@naren-innowell:registry=https://npm.pkg.github.com
# Token is only needed when publishing or installing from GitHub Packages
# Uncomment the line below and set GITHUB_TOKEN env var if needed for local publishing
# //npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}